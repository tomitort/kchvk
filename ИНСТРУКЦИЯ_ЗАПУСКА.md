# 🚀 Self-Deploy CI/CD - Полное руководство по запуску

## 📋 О проекте

**Self-Deploy CI/CD** - это система автоматической генерации CI/CD конфигураций для Jenkins и GitLab CI. Поддерживает анализ репозиториев на 4 языках:
- **Java** (Maven, Gradle)
- **Go** (Go Modules)
- **JavaScript/TypeScript** (npm, yarn)
- **Python** (pip, poetry)

---

## 🛠️ Предварительные требования

### Docker (обязательно)

#### Windows:
1. Скачайте **Docker Desktop** с https://www.docker.com/products/docker-desktop
2. Установите с настройками по умолчанию
3. Включите WSL 2 в настройках Docker Desktop
4. Перезагрузите компьютер

#### Linux (Ubuntu/Debian):
```bash
# Установка Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh

# Добавление пользователя в группу docker
sudo usermod -aG docker $USER

# Установка Docker Compose
sudo curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
sudo chmod +x /usr/local/bin/docker-compose

# Перезапуск сессии
newgrp docker
```

#### macOS:
1. Скачайте **Docker Desktop** с https://www.docker.com/products/docker-desktop
2. Установите .dmg файл
3. Запустите Docker Desktop из Applications

---

## 🚀 Быстрый старт (3 способа запуска)

### Способ 1: Локальный запуск (рекомендуется для разработки)

#### Шаг 1: Установка зависимостей
```bash
# Клонирование репозитория
git clone <repository-url>
cd self-deploy-ci-cd

# Установка Python зависимостей
pip install -r requirements.txt
```

#### Шаг 2: Запуск анализа репозитория
```bash
# Анализ реального репозитория
python main.py --repo https://github.com/user/project --system both --verbose

# Демонстрационный режим
python main.py --demo

# Показать справку
python main.py --help
```

#### Доступные параметры:
- `--repo <url>`: URL репозитория для анализа
- `--system <jenkins|gitlab|both>`: Целевая CI/CD система
- `--output <dir>`: Выходная директория (по умолчанию: ./output)
- `--verbose`: Подробный режим
- `--demo`: Демонстрационный режим с тестовыми проектами

### Способ 2: Запуск через Docker

#### Шаг 1: Сборка образа
```bash
docker build -t self-deploy-ci-cd .
```

#### Шаг 2: Запуск контейнера
```bash
# Анализ репозитория
docker run -v $(pwd)/output:/home/app/output self-deploy-ci-cd \
  --repo https://github.com/user/project --system both

# Демонстрационный режим
docker run -v $(pwd)/output:/home/app/output self-deploy-ci-cd --demo

# Показать справку
docker run self-deploy-ci-cd --help
```

### Способ 3: Полная CI/CD инфраструктура

#### Шаг 1: Запуск всей инфраструктуры
```bash
cd infrastructure
docker-compose up -d
```

#### Шаг 2: Ожидание запуска (5-10 минут)
Сервисы будут доступны по адресам:

| Сервис | URL | Порт | Описание |
|--------|-----|------|----------|
| **Jenkins** | http://localhost:8080 | 8080 | CI/CD сервер |
| **GitLab** | http://localhost:8081 | 8081 | Git репозиторий + CI/CD |
| **SonarQube** | http://localhost:9000 | 9000 | Анализ кода |
| **Nexus** | http://localhost:8082 | 8082 | Репозиторий артефактов |
| **Portainer** | http://localhost:9001 | 9001 | Управление Docker |

#### Шаг 3: Настройка после запуска

**Jenkins:**
1. Откройте http://localhost:8080
2. Дождитесь установки плагинов (автоматически)
3. Создайте пайплайн используя сгенерированный Jenkinsfile

**GitLab:**
1. Откройте http://localhost:8081
2. Войдите с логином `root` и паролем `admin123`
3. Создайте проект и добавьте .gitlab-ci.yml

**SonarQube:**
1. Откройте http://localhost:9000
2. Войдите с логином `admin` и паролем `admin`
3. Смените пароль при первом входе

---

## 📊 Примеры использования

### Анализ Go проекта
```bash
python main.py --repo https://github.com/syncthing/syncthing --system both --verbose
```

### Анализ Java проекта
```bash
python main.py --repo https://github.com/jenkinsci/jenkins --system jenkins --verbose
```

### Анализ JavaScript проекта
```bash
python main.py --repo https://github.com/RocketChat/Rocket.Chat --system gitlab --verbose
```

### Анализ Python проекта
```bash
python main.py --repo https://github.com/django/django --system both --verbose
```

---

## 🛠️ Управление инфраструктурой

### Основные команды Docker Compose:
```bash
# Остановка всех сервисов
docker-compose down

# Перезапуск
docker-compose restart

# Просмотр логов
docker-compose logs -f

# Просмотр статуса
docker-compose ps

# Полная очистка (удалит все данные)
docker-compose down -v
```

### Очистка временных файлов:
```bash
# Windows (PowerShell)
Remove-Item -Recurse -Force $env:TEMP\self_deploy_*

# Linux/macOS
rm -rf /tmp/self_deploy_*
```

---

## 🔧 Решение проблем

### Проблема: Порты заняты
Измените порты в файле `infrastructure/docker-compose.yml`:
```yaml
ports:
  - "8083:8080"  # Вместо 8080:8080
  - "8084:8081"  # Вместо 8081:8081
```

### Проблема: Недостаточно места на диске
```bash
# Очистка Docker кэша
docker system prune -a

# Очистка временных файлов
docker volume prune
```

### Проблема: Медленный запуск GitLab
GitLab требует 5-10 минут для первой инициализации. Проверьте логи:
```bash
docker-compose logs gitlab -f
```

### Проблема: Ошибки клонирования репозиториев
```bash
# Используйте shallow clone для больших репозиториев
git clone --depth 1 <repository-url>

# Или увеличьте глубину клонирования
python main.py --repo <url> --depth 5
```

---

## 📁 Структура проекта

```
self-deploy-ci-cd/
├── src/                    # Исходный код
│   ├── analyzers/         # Анализаторы репозиториев
│   ├── generators/        # Генераторы конфигураций
│   ├── templates/         # Шаблоны CI/CD
│   └── utils/            # Вспомогательные утилиты
├── infrastructure/        # Docker Compose инфраструктура
├── examples/             # Тестовые проекты
├── output/               # Сгенерированные конфигурации
├── main.py              # Основной скрипт
├── Dockerfile           # Конфигурация Docker
└── requirements.txt     # Зависимости Python
```

---

## 📞 Поддержка

Если возникли проблемы:

1. **Проверьте логи**: `docker-compose logs [service]`
2. **Убедитесь что порты свободны**
3. **Проверьте доступное место на диске**
4. **Перезапустите**: `docker-compose down && docker-compose up -d`

**Документация:**
- [README.md](./README.md) - Основная документация
- [DOCUMENTATION.md](./DOCUMENTATION.md) - Техническая документация
- [TEST_REPORT.md](./TEST_REPORT.md) - Отчет о тестировании

---

## 🎯 Готовые примеры

Проект включает готовые примеры в директории `examples/`:

- `test_java_project/` - Maven проект с тестами
- `test_go_project/` - Go модуль с тестами  
- `test_js_project/` - Node.js проект с Jest
- `test_python_project/` - Python проект с pytest

**Готово!** Система запущена и готова к использованию! 🚀